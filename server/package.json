{
  "name": "terrace-server",
  "version": "0.1.0",
  "private": true,
  "description": "Terrace realtime server: watch-party rooms, reactions, polls, rivalry, live match replay.",
  "scripts": {
    "dev": "ts-node --transpile-only src/server.ts",
    "build": "tsc --noEmit"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "@types/ws": "^8.5.10",
    "ts-node": "^10.9.2",
    "typescript": "^5.5.3"
  }
}
